name: CI

on:
  push:
    branches: [ "master", "ci/*" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  test:
    name: test py${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.9", "3.10", "3.11", "3.12", "3.13" ]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Go environment
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: tests/clients/go/go.sum
      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 22
          cache: maven
          cache-dependency-path: tests/clients/java/pom.xml
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}
      - name: Install libmaxminddb from source
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential autoconf automake libtool
          git clone --depth 1 --branch 1.13.2 https://github.com/maxmind/libmaxminddb.git /tmp/libmaxminddb
          cd /tmp/libmaxminddb
          ./bootstrap && ./configure && make -j$(nproc) && sudo make install
          sudo ldconfig
      - name: Test
        run: uv run --python ${{ matrix.python-version }} --extra test pytest
      - name: Lint
        if: matrix.python-version == '3.13'
        run: |
          uv run --python ${{ matrix.python-version }} --extra dev ruff check --no-fix
          uv run --python ${{ matrix.python-version }} --extra dev ruff format --check
